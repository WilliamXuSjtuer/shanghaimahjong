# 🀄 麻将多人联机 - Railway 部署指南

## 🚀 一键部署到 Railway

### 方法一：从 GitHub 子目录部署（推荐）

由于 Railway 不直接支持部署子目录，我们需要创建一个独立仓库：

#### 步骤 1：创建独立仓库
```bash
# 创建新目录
mkdir mahjong-multiplayer-deploy
cd mahjong-multiplayer-deploy

# 复制文件
cp -r ../games/mahjong-multiplayer/* .
mkdir -p img
cp ../games/mahjong/img/majiang.png img/

# 初始化 Git
git init
git add .
git commit -m "麻将多人联机服务器"

# 推送到新仓库
git remote add origin https://github.com/你的用户名/mahjong-multiplayer.git
git push -u origin main
```

#### 步骤 2：在 Railway 部署
1. 访问 [railway.app](https://railway.app)
2. 登录（使用 GitHub 账号）
3. 点击 **New Project** → **Deploy from GitHub repo**
4. 选择 `mahjong-multiplayer` 仓库
5. Railway 会自动检测 Node.js 项目
6. 点击 **Deploy**
7. 等待 2-3 分钟部署完成

#### 步骤 3：获取游戏链接
部署完成后，点击 **Settings** → **Domains** → **Generate Domain**

你会获得类似这样的链接：
```
https://mahjong-multiplayer-xxx.railway.app
```

### 方法二：使用 Railway CLI

```bash
# 安装 Railway CLI
npm install -g @railway/cli

# 登录
railway login

# 进入项目目录
cd games/mahjong-multiplayer

# 初始化项目
railway init

# 部署
railway up
```

## 📱 邀请好友对战

1. 打开部署后的链接
2. 输入昵称
3. 点击 **创建房间**
4. 把 **6位房间号** 发给朋友
5. 朋友输入房间号 **加入房间**
6. 全部准备后自动开始游戏！

## 🎮 游戏规则

- **玩法**: 上海敲麻（简化版）
- **人数**: 1-4人（不足4人用AI填充）
- **牌型**: 万、条、筒各36张，共108张
- **目标**: 胡牌获胜（碰、杠、胡）

## 🔧 环境变量（可选）

| 变量 | 说明 | 默认值 |
|------|------|--------|
| PORT | 服务器端口 | 3000 |
| NODE_ENV | 环境 | production |

## 📊 Railway 免费额度

- ⏰ **500小时/月** 运行时间
- 🔒 **自动 HTTPS**
- 🚀 **自动休眠/唤醒**
- 📈 **自动扩容**

## 🛠️ 故障排除

### 部署失败
检查 Railway 部署日志，常见问题：
- `package.json` 格式错误
- 依赖安装失败

### 无法连接
- 确保使用 HTTPS 链接
- 检查浏览器控制台错误
- 尝试刷新页面

### 手机无法打开
- 使用现代浏览器（Chrome/Safari）
- 确保网络正常

---

**🎉 祝你麻将大吉！** 🀄✨

